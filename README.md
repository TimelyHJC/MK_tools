# MK-tool 使用说明（中文版）

`MK-tool` 是一个浏览器扩展，主要用于门店资料下载、三图资料整理、字段提取，以及钉钉表单同步。

本文档以“日常使用” 为主，不是开发说明。

## 这个插件能做什么

- 一键提取当前页面里的图片和附件并下载
- 下载“三图资料”
- 自动识别页面中的门店信息
- 把识别到的字段同步到钉钉表单
- 打开预先配置好的钉钉表格地址
- 管理默认文件
- 维护门店编码与序号的对应关系

## 适用浏览器

- Chrome
- Edge
- 其他 Chromium 内核浏览器

## 安装方法

1. 打开扩展管理页
   - Chrome：`chrome://extensions`
   - Edge：`edge://extensions`
2. 打开“开发者模式”
3. 点击“加载已解压的扩展程序”
4. 选择当前项目文件夹 `MK_tools`

## 第一次使用前要做的设置

打开扩展设置页后，建议按下面顺序配置。

### 1. 默认文件

可以上传一些固定会跟门店资料一起存放的文件。

用途：

- 新建门店资料目录时自动带上这些文件
- 适合放模板、清单、说明文件等

你可以设置：

- 默认文件存放文件夹名
- 默认文件列表

如果不填“默认文件存放文件夹”，默认文件会直接放进门店目录下。  
如果填写了文件夹名，默认文件会放进该子文件夹里。

### 2. 序号门店映射

这个区域用来维护：

- 月份
- 序号
- 门店编码
- 上匠门店简称

支持：

- 手动编辑
- 导入表格
- 导出表格
- 清空数据

当前规则：

- 同一个门店编码，只会在第一次下载时写入映射
- 以后再次下载，会直接复用原来的序号、月份和门店简称
- 后续下载不会因为跨月而自动改成新月份
- 如果导入表格里已经有该门店编码，后面下载时不会重复新增，也不会覆盖原记录

### 3. 钉钉信息设置

钉钉地址配置区域放在设置页的最后面。

需要配置两项：

- 钉钉表单地址：用于“同步表单”
- 钉钉表格地址：用于“打开表单”

如果不配置，页面里的相关按钮将无法正常使用。

## 页面里怎么使用

进入目标页面后，插件会注入一个悬浮面板。

快捷键：

```text
Alt + Q
```

可用于打开或关闭悬浮面板。

## 悬浮面板按钮说明

### 提取字段

插件会尽量自动识别当前页面中的门店信息，并显示在面板下方。

常见提取字段包括：

- 门店编码
- 上匠门店简称
- 加盟商名称
- 加盟商联系方式
- 预估流水
- 外卖占比
- 外卖流水
- 完整地址
- 项目类型
- 省份
- 拓展顾问
- 接案时间
- 店宽
- 装修面积
- 完成时间
- 状态

如果没有提取到门店编码，部分下载和同步功能会受到影响。

### 一键提取并下载

这个按钮用于下载当前页面中可识别的图片和文件。

适合场景：

- 勘场资料下载
- 页面附件归档
- 按门店整理现场资料

下载时会优先结合：

- 当前门店编码
- 上匠门店简称
- 已存在的门店映射
- 当前序号

来生成对应目录。

### 三图资料下载

这个按钮用于下载页面中的“三图相关文件”，例如：

- 效果图
- 施工图
- 三图确认相关附件

下载结果会放到对应门店目录下的“三图资料”相关目录中。

### 同步表单

这个按钮会：

1. 使用当前页面已提取到的字段
2. 打开你配置好的钉钉表单地址
3. 自动尝试填入字段

使用前提：

- 已成功提取到页面字段
- 已在设置页填写“钉钉表单地址”

### 打开表单

这个按钮只负责打开设置页里配置的“钉钉表格地址”，方便你快速跳转。

使用前提：

- 已在设置页填写“钉钉表格地址”

## 下载目录规则

插件会根据门店信息自动创建下载目录。

常见命名规则为：

```text
序号--门店编码上匠门店简称
```

在这个门店目录下，再放入不同资料，例如：

- 现场资料
- 三图资料
- 默认文件

如果你在设置页中填写了“默认文件存放文件夹”，默认文件会进入这个子目录；否则直接放在门店主目录下。

## 门店映射规则

这是当前非常重要的一条规则：

- 首次下载某个门店编码时，才会写入映射
- 之后再次下载同一个门店编码，会复用原来的记录
- 不会因为到了新月份就自动改月份
- 不会因为再次下载就覆盖原来的门店简称
- 如果列表里已经有同门店编码，就不再新增

也就是说：

如果某门店最早是在 3 月导入或下载建立的记录，后面 4 月、5 月再次下载时，仍然保持原来的月份和序号，不会被自动改掉。

## 推荐使用流程

日常使用建议按下面顺序操作：

1. 先打开目标页面
2. 按 `Alt + Q` 打开悬浮面板
3. 先检查字段是否提取正确
4. 如需下载现场资料，点击“一键提取并下载”
5. 如需下载三图资料，点击“三图资料下载”
6. 如需同步钉钉，点击“同步表单”
7. 如只需跳转查看钉钉地址，点击“打开表单”

## 常见问题

### 1. 按钮点了没反应

先检查：

- 当前页面是否已经刷新到最新
- 扩展是否刚更新过
- 设置页中的钉钉地址是否已填写

如果你刚改过代码或重新加载过扩展，请：

1. 在扩展管理页点击“重新加载”
2. 回到目标网页刷新页面
3. 再重新操作

### 2. 下载目录不对

先检查：

- 当前页面是否成功识别到门店编码
- 序号门店映射表中是否已经存在该门店编码

如果该门店编码已经存在，插件会优先复用旧映射，不会自动按新月份覆盖。

### 3. 三图资料没有进对应目录

通常与以下情况有关：

- 门店编码未识别成功
- 映射表中已有旧记录并被复用
- 浏览器下载路径设置影响了最终保存位置

建议先检查当前门店编码和映射表内容是否一致。

### 4. 同步表单失败

先检查：

- 钉钉表单地址是否填写正确
- 当前页面字段是否提取成功
- 表单页面结构是否发生变化

如需进一步查看表单同步设计，可参考：

- [表单同步优化说明](./docs/%E8%A1%A8%E5%8D%95%E5%90%8C%E6%AD%A5%E4%BC%98%E5%8C%96%E8%AF%B4%E6%98%8E.md)

## 版本信息

当前仓库对应扩展版本：

```text
1.1.3
```
